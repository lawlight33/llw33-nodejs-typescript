### Node.js for TypeScript quick start

#1. Compile TypeScript to JavaScript:
```shell
npx tsc
```

#2. Run the JS code through Node:
```shell
node index.js
```

Consider this one-liner as well:
```shell
npx ts-node index.ts
```